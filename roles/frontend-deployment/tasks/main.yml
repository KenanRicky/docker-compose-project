---
- name: Run database container_name
  docker_container:
    name: app-mongo
    image: mongod
    ports:
       - "27017:27017"
    volumes:
      - ./app-mongo-data:/data/db
    networks:
      - name: app-net
  tags:
    - database
    - backend
